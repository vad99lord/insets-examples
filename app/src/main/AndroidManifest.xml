<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vad99lord.insets_examples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Insetsexamples"
        tools:ignore="AllowBackup">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Insetsexamples.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".compose.samples.basic_list.ListSampleActivity"/>
        <activity android:name=".compose.samples.gestures.GestureExclusionSampleActivity"/>
        <activity android:name=".compose.samples.gestures.MandatoryGestureSampleActivity"/>
        <activity android:name=".compose.samples.keyboard.KeyboardBasicImeSampleActivity"/>
        <activity android:name=".compose.samples.keyboard.KeyboardScrollImeSampleActivity"/>
        <activity android:name=".compose.samples.fullscreen.FullscreenCutoutSampleActivity"/>
        <activity android:name=".compose.samples.fullscreen.FullscreenStableSampleActivity"/>
    </application>

</manifest>